# build env to run build independently
name: mlc-llm-build

channels:
  - conda-forge

dependencies:
  - conda-build
  - anaconda-client
  - libvulkan-headers
  - libvulkan-loader
  - spirv-tools
  - git
  - cmake
  - bzip2
